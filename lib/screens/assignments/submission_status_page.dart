import 'package:flutter/material.dart';

class SubmissionStatusPage extends StatelessWidget {
  const SubmissionStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Submission Status')),
      body: const Center(child: Text('Submission Status Page')),
    );
  }
}

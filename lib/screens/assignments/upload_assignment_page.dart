import 'package:flutter/material.dart';

class UploadAssignmentPage extends StatelessWidget {
  const UploadAssignmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Upload Assignment')),
      body: const Center(child: Text('Upload Assignment Page')),
    );
  }
}

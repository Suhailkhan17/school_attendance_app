import 'package:flutter/material.dart';

class EnterMarksPage extends StatelessWidget {
  const EnterMarksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enter Marks')),
      body: const Center(child: Text('Enter Marks Page')),
    );
  }
}
